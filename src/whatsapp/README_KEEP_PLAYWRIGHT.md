This project previously used a custom Playwright-based WhatsApp Web client in `client.ts` and `selectors.ts`.

These files are now unused in favour of `whatsapp-web.js` via `wwebClient.ts`. They are kept temporarily for reference during the transition and can be deleted once you are confident in the new transport.

